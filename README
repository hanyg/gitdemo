git demo


bundle exec asciidoctor-revealjs -a revealjsdir=https://cdn.jsdelivr.net/npm/reveal.js@3.9.2  $File


asciidoctor-pdf -a pdf-themesdir=resources/themes -a pdf-theme=custom  -r asciidoctor-lists  git\ overview.adoc

sudo ruby -run -e httpd . -p 5000 -b 127.0.0.1

sudo needed on Unix systems
